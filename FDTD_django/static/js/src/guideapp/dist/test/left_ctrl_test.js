// Generated by CoffeeScript 1.9.2
(function() {
  define(['angular', 'angularMocks', 'controllers/index', 'services/index', 'app'], function() {
    return describe('leftCtrl', function() {
      beforeEach(module('app.controllers'));
      beforeEach(module('app'));
      beforeEach(inject(function(_$controller_, _$location_, _$rootScope_) {
        this.$controller = _$controller_;
        this.$location = _$location_;
        return this.$rootScope = _$rootScope_;
      }));
      it('should exist', function() {
        var $scope, controller;
        $scope = {};
        $scope.$on = function() {
          return 1;
        };
        controller = this.$controller('LeftCtrl', {
          $scope: $scope
        });
        return should.exist(controller);
      });
      it('selectsubmenu', function() {
        var $scope, navmenu;
        $scope = {};
        $scope.$on = function() {
          return 1;
        };
        this.$controller('LeftCtrl', {
          $scope: $scope
        });
        navmenu = {
          'menu': 'test2'
        };
        $scope.selectsubmenu('test1', navmenu);
        assert.equal(this.$location.url(), '/test2/test1');
        assert.equal($scope.submenuselected, 'test1');
        return assert.equal($scope.menuselected, 'test2');
      });
      it('selectmenu', function() {
        var $scope, navmenu;
        $scope = {};
        $scope.$on = function() {
          return 1;
        };
        this.$controller('LeftCtrl', {
          $scope: $scope
        });
        navmenu = {
          'menu': 'test2'
        };
        $scope.selectmenu(navmenu);
        return assert.equal($scope.collapsemenuselected, 'test2');
      });
      return it('generateversionarray', function() {
        var $scope, controller, data, releaseversions;
        $scope = {};
        $scope.$on = function() {
          return 1;
        };
        controller = this.$controller('LeftCtrl', {
          $scope: $scope
        });
        data = [
          {
            'version': '0.1.0'
          }, {
            'version': '0.1.1'
          }
        ];
        releaseversions = controller.generateversionarray(data);
        assert.equal(releaseversions.length, 2);
        return assert.equal(releaseversions[0], '0.1.0');
      });
    });
  });

}).call(this);

//# sourceMappingURL=left_ctrl_test.js.map
